{
  "name": "simple-weather",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "firebase": "^5.5.8",
    "node-sass": "^4.10.0",
    "normalize.css": "^8.0.1",
    "react": "^16.6.3",
    "react-dom": "^16.6.3",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "firebase-init": "firebase login && firebase init",
    "deploy": "npm run build && firebase deploy",
    "setup-env": "firebase functions:config:set simple-weather.react_app_key=process.env.REACT_APP_OPEN_WEATHER_API_KEY"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "dotenv": "^6.1.0",
    "firebase-tools": "^6.1.0"
  }
}
